// Memory.Wiki iOS — secrets template. Copy to Secrets.xcconfig
// (gitignored) and fill in the values from the web app's
// .env.local (apps/web/.env.local) — same NEXT_PUBLIC_* keys.

MEMORY_WIKI_SUPABASE_URL = https:/$()/your-project.supabase.co
MEMORY_WIKI_SUPABASE_ANON_KEY = your-anon-key
